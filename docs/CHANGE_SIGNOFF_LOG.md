# Change Sign-off Log — EarnedHome

Running record of changes that need **Richard's (and where noted, counsel's) sign-off before they reach production** (the live Netlify site). Nothing on this list goes live until its status is **Approved → Live**.

**Status flow:** `Draft (dev)` → `In preview` (private link sent for review) → `Approved` (Richard/counsel OK'd) → `Live` (merged to `main`, deployed).

| # | Date | Change | Files / area | Branch | Status | Approved by / date |
|---|------|--------|--------------|--------|--------|--------------------|
| 1 | 2026-06-09 | Replaced placeholder buyer-tool disclosures with **R Parry's canonical rate-disclosure language** (advertisement/not-an-LE, APR definition, rate variability, MI & tax/insurance/flood assumptions, not-a-credit-decision, nontraditional products, licensing). Centralized as one shared source used by both pricing engines. Surfaces **NMLS #1924318 (LLC)** and **#927662 (Richard)** + Equal Housing. | `src/lib/pricing/disclosures.ts` (new), `stub.ts`, `graph.ts` | `dev` | **Draft — pending Richard preview + sign-off** | — |
| 2 | 2026-06-24 | **Loan eligibility tiers + greyed-out ineligible cards.** Conventional conforming + **Jumbo Tier 1/2** (680/700 credit, 89.99%/80% LTV) + >$3.5M LO-only; FHA ≤$573,361; **VA + VA-Jumbo** tiers (640/680 credit, >$2.5M ineligible). Ineligible products shown greyed with the reason. **Richard must confirm the limits/credit floors/LTV caps match his lending matrix** before production. | `src/lib/eligibility.ts`, `src/components/PathfinderTool.tsx` | `dev` | **Draft — pending Richard confirm numbers** | — |
| 3 | 2026-06-24 | **Seller Credit hidden** — default to $0 and remove from the buyer view in 1A (logic retained for later). Decided with Marvin; *not yet built.* | `src/components/PathfinderTool.tsx` (planned) | — | **Planned — to build** | — |
| 4 | 2026-06-25 | **Forgot-password (loan-officer only)** — built + verified locally; hidden behind `NEXT_PUBLIC_ENABLE_PASSWORD_RESET` (off). **GATE before enabling in production:** (a) **Resend SMTP** configured in Supabase (built-in email is rate-limited — *Resend deferred per Marvin June 25*), (b) QA+prod redirect URLs added, (c) recommended `token_hash` recovery template for cross-device. Buyers have no accounts; not buyer-facing. | `src/app/login/page.tsx`, `src/app/reset-password/page.tsx`, `src/app/auth/callback/route.ts` | `dev` | **Flag-OFF — blocked on Resend SMTP before go-live** | — |
| 5 | 2026-06-25 | **Jumbo Tier 1 "down needed" wording** — greyed-card message now reads **"needs at least 10.01% down"** (true 89.99% LTV minimum) instead of a misleading "10%". Buyer-facing copy. | `src/lib/eligibility.ts` | `dev` | **Draft — Richard FYI/confirm with A2 (eligibility)** | — |
| 6 | 2026-06-25 | **Per-tenant identity in disclosure** — disclosure identity line will render the tenant's `legal_name` / `company_nmls` / `originator_name` / `nmls` (R Parry values seeded via `0005`). Richard to confirm the values are correct. Legal prose unchanged. | `supabase/migrations/0005_tenant_identity.sql` (+ pending disclosure-templating code) | `dev` (DB applied) | **Draft — pending Richard confirm values** | — |
| 7 | 2026-06-29 | **Buyer term/definition + disclosure copy update** ("Understanding your estimate" panel + input tooltips). Expanded **Mortgage Insurance** (conventional >80% LTV; credit/LTV-based pricing; FHA UFMIP + monthly MIP; VA funding fee + disability waiver — corrects prior text that wrongly said VA has no MI). Added **RESPA/TILA "this is NOT a Loan Estimate"** disclaimer under Estimated Funds. New **Seller Credit** and **Military/Veteran** tooltips (the latter links to **va.gov** eligibility). Added "consult your licensed Loan Officer" to Less Seller Contribution and Estimated Total. All buyer-facing. | `src/components/PathfinderTool.tsx` | `dev` | **Draft — pending Richard RESPA review of copy** | — |
| 8 | 2026-06-29 | **Buyer-estimate email (Resend)** — on lead submit, emails the buyer their eligibility-aware estimate (both 30/15-yr terms, per-product cash-to-close, scenario box) + disclosures. **Ships dormant:** no-ops unless `RESEND_API_KEY` / `RESEND_FROM` are set, so it is safe on QA with no env config and will not send. **GATES before it sends in any environment:** (a) verified Resend from-domain + API key, (b) **Richard RESPA review of the buyer email copy/disclosures.** | `src/lib/email.ts` (new), `src/app/api/lead/route.ts`, `src/components/PathfinderTool.tsx`, `.env.example` | `dev` | **Dormant on QA — blocked on Resend config + Richard copy sign-off before sending** | — |
| 9 | 2026-07-06 | **Emails now SENDING on QA** — Resend configured (`RESEND_API_KEY` + verified `RESEND_FROM` = `thetimmonsfoundation.org`). Buyer estimate email, LO lead alert, and (new) agent copy all fire on submit. Serverless fix: side effects `await Promise.allSettled` before the function freezes (was intermittently cut off). **Before real Prod buyers:** `notify_email` → Richard, and Richard RESPA sign-off on live buyer/LO/agent copy. | `src/app/api/lead/route.ts`, `src/lib/email.ts` | `dev` (+ `main` for the connect flow) | **Sending on QA — needs notify_email→Richard + copy sign-off before Prod buyers** | — |
| 10 | 2026-07-06 | **Phase 1A Agent (Realtor) Attribution** — per-agent `/a/<slug>` links tag a buyer to a realtor; buyer sees "Your agent"; lead stores `agent_id`; LO alert says "(via agent)"; agent gets a copy. Dashboard Agents page (add/edit/copy-link/email-link/on-off/filter); revoked link is blocked; leads show agent + DISABLED badge. Seat-based (LO gives/revokes). | `supabase/migrations/0008–0010`, `src/lib/agents.ts`, `src/app/api/admin/agents/**`, `src/app/a/[slug]/`, `src/app/dashboard/agents/**`, `src/app/dashboard/{page,LeadsTable}.tsx`, `src/components/PathfinderTool.tsx`, `src/lib/email.ts` | `dev` | **On QA — engineering (no copy sign-off); agent emails share the copy-review gate (#9)** | — |
| 11 | 2026-07-06 | **Buyer connect UX** — name/email(+format)/phone now **required** to connect and to update; **"Update my info"** after connecting edits the same lead (no duplicate); connected screen shows the **LO phone** + a **Calendly reschedule** note; buyer email ends with a **"Book a time"** button + LO phone. Buyer-facing copy. | `src/components/PathfinderTool.tsx`, `src/app/api/lead/contact/route.ts` (new), `src/lib/email.ts` | `dev` | **Draft — pending Richard review of buyer-facing copy** | — |
| 12 | 2026-07-06 | **Company identity** — header/branding line + footer NMLS set to company **1924318** (header via `branding.tag`; footer `nmls` column — *footer currently still 927662; decision pending which number the EHO footer shows*). Agent-list phone formatting `(XXX) XXX-XXXX`. | `tenants` row (data), `src/components/PathfinderTool.tsx`, `src/app/dashboard/agents/AgentsManager.tsx` | `dev` + shared DB | **Data set — footer NMLS choice pending** | — |

| 13 | 2026-07-08 | **Email domain go-live — `rparryfinancial.com`.** Verified as the Resend sending domain (DKIM/SPF/MX on `send.` subdomain; DNS at Network Solutions via Raise Your Media). `RESEND_FROM` flipped to **`R Parry Financial <no-reply@rparryfinancial.com>`** on all Netlify contexts + `.env.local`. `thetimmonsfoundation.org` removed (free tier = 1 domain). Prod rebuilt to apply. | Netlify env, Resend, DNS | `main` + `dev` | **Live** | — |
| 14 | 2026-07-08 | **Branded production URL — `home.rparryfinancial.com`.** Custom domain added on Netlify (CNAME `home` → `earnedhome.netlify.app` + ownership TXT), SSL issued (Let's Encrypt). Serves the production (`main`) app. | Netlify domain, DNS | `main` | **Live** | — |
| 15 | 2026-07-08 | **Dashboard closed/funded production tracking.** "Closed" status renamed **"Closed / Funded"**; `closed_at` stamped on close (`0011`); new metric cards **Closed / Funded**, **Closed this month**, **Lead → Closed %**, **Close rate (this month)** in a counts-row + rates-row layout (funnel order); metric cards refresh live on status change. LO-facing. | `src/app/dashboard/{page,LeadsTable}.tsx`, `src/app/api/lead/update/route.ts`, `supabase/migrations/0011` | `dev` | **On QA — engineering + LO-facing labels; Richard FYI** | — |
| 16 | 2026-07-09 → 07-16 | **Phase II — multi-LO + broker roll-up.** Branch model moved to **`rel` → `test` (QA) → `main` (Prod)**. Multi-LO foundation; leads route through the agent's LO (`assigned_lo_id` snapshot); **role-aware RLS** so a non-admin LO sees only their own leads, admins see all (`0012–0016`); LO management page (add/edit/invite); agent status portal `/agent/<token>`; shared **DashHeader**. Three branded **role manuals** (Broker/LO/Agent) produced for Richard. | `src/lib/loanOfficer.ts`, `loSelect.ts`, `src/app/dashboard/los/**`, `src/app/agent/[token]/**`, `supabase/migrations/0012–0016` | `rel`/`test` (QA) | **On QA — engineering + LO-facing; Richard FYI (manuals delivered)** | — |
| 17 | 2026-07-16 → 07-20 | **Buyer referral + agent-invite share system (Flow A/B).** New `share_links` table + `leads.referred_by` (`0019`). **Flow A:** an agent invites a buyer (emailed per-buyer link), can turn any buyer's link **off/on**, and re-send it ("Email link"). **Flow B:** a buyer shares with a friend via a form → the friend is emailed the estimate link; anyone who completes follows the upline (agent → LO), tagged `buyer_referral` with `referred_by` = the sharer. Agent alert reframed around the agent's root client. **Compliance:** referrals are **buyer-initiated only** (RESPA/CAN-SPAM posture); **"Financing by [LO], NMLS · [Company], NMLS. Equal Housing Lender"** email footer — **counsel to confirm wording** before real buyers. | `supabase/migrations/0019`, `src/lib/shareLinks.ts`, `src/lib/email.ts`, `src/app/api/{agent,share}/**`, `src/app/{r,share}/[token]/**`, `src/app/agent/[token]/**`, `src/components/PathfinderTool.tsx` | `rel`/`test` (QA) | **On QA — pending counsel confirm of referral-email footer + RESPA posture before Prod buyers** | — |
| 18 | 2026-07-22 | **Buyer-form + email polish (today).** (a) Agent portal shows an **invited buyer's email on its own line** (reach out before they run numbers). (b) **"Share with a friend"** button + link centered in the estimate email. (c) Referral email now **names the sender** — "A friend, {name}, thought you'd want to see this" (heading + subject). (d) **FHA & VA restricted to primary residence** — ineligible for **Second Home and Investment** (engine rule). (e) **Military/Veteran ↔ occupancy two-way lock** in the buyer form (Veteran available for Primary only; each disables the other). | `src/lib/eligibility.ts`, `src/components/PathfinderTool.tsx`, `src/lib/email.ts`, `src/app/api/share/send/route.ts`, `src/app/agent/[token]/AgentActions.tsx` | `rel`/`test` (QA) | **On QA — Richard to confirm FHA/VA = primary-only vs lending matrix; referral-email copy under counsel gate (#17)** | — |
| 19 | 2026-08-25 | **Connect CTA + connected-confirmation copy.** Buyer "Connect" CTA finalized to **"Connect with my mortgage advisor"**; connected screen reworded to **"You're all set, [name]"** + **"[Advisor] with [company] [phone] will contact you shortly…"** + **"Our goal is simple: to provide clear answers so you can move forward with confidence."** Buyer-facing. | `src/components/PathfinderTool.tsx` | `rel`/`test`/`main` | **Live — copy under Richard/counsel review** | — |
| 20 | 2026-08-25 | **Rebrand "Loan Officer" → "Mortgage Advisor" system-wide (CR-004)** — all user-facing copy (buyer pages, dashboard, login, emails, disclosures). Internal `lo` role + `/dashboard/los` URL unchanged (not user-visible). | `src/components/PathfinderTool.tsx`, `src/lib/email.ts`, dashboard/login pages | `rel`/`test`/`main` | **Live — ⚠️ Richard to confirm his state permits "licensed Mortgage Advisor" in disclosures ("loan officer" is the NMLS-licensed title)** | — |
| 21 | 2026-08-25 | **EHO footer line** — changed to **"Equal Housing Opportunity · Estimates only, not a commitment to lend."**; **NMLS removed** from that footer line. Buyer-facing disclosure copy. | `src/components/PathfinderTool.tsx` | `rel`/`test`/`main` | **Live — Richard/counsel FYI (resolves the earlier "which NMLS in the footer" question by removing it)** | — |
| 22 | 2026-08-25 | **Buyer estimate email detail** — adds **APR** (shown as **Rate / APR**), renames to **Estimated Payment** and **Estimated Cash to Close**, and adds the **Loan amount**. Buyer-facing email. | `src/lib/email.ts` | `rel`/`test`/`main` | **Live — under the email-copy review gate (#9)** | — |
| 23 | 2026-08-25 | **Rebrand EarnedHome → BuyerBridge system-wide (CR-001/003)** — wordmark + new logos (`public/brand/buyerbridge-logo*.png`), **white header bar with true-color logo + blue bottom rule**, **blue nav pills** for white nav text, consistent logo placement on every page, **"Powered by…" line removed**. Internal slug (`earnedhome`) and repo/Netlify names unchanged (not user-visible). | `src/app/globals.css`, `DashHeader`, `BrandHeader`, agent/wizard/readiness headers, `public/brand/*`, `src/lib/email.ts`, `PathfinderTool.tsx` | `rel`/`test`/`main` | **Live — 🛑 trademark risk on record (CR-001): "BUYERBRIDGE" is a LIVE US mark (Reg. 6003449, Dealers United, LLC) — counsel to clear the name + secure a domain before external launch** | — |
| 24 | 2026-08-25 | **Agent firm/brokerage field (migration 0020)** — `agents.firm`; admin Agents form + CSV **FIRM** column; buyer page shows **"Your agent: [name] with [firm]"** incl. a top-of-form banner on open. Plus a per-agent **"Download list (CSV)"** export (FNAME, LNAME, FIRM, EMAIL, PHONE, referral Link, Portal link). | `supabase/migrations/0020_agents_firm.sql`, `src/lib/agents.ts`, `src/app/api/admin/agents/**`, `AgentsManager.tsx`, `src/app/a/[slug]/page.tsx`, `PathfinderTool.tsx` | `rel`/`test`/`main` | **Live (0020 applied — firm saves/displays)** | — |
| 25 | 2026-08-25 | **Agent-invite email reword** — heading + subject to **"Your path to home starts here"** (warmer; removes affordability doubt). | `src/lib/email.ts` | `rel`/`test`/`main` | **Live — under the email-copy review gate (#9/#17)** | — |
| 26 | 2026-08-25 | **Daily Broadcast — Stage 1 (recipient store).** New **Contacts** model — `contacts`, `broadcasts`, `broadcast_recipients`, `email_unsubscribes` (migration **0021**, RLS = service-role only); admin **Contacts** page (`/dashboard/contacts`) to import a CSV **once** + add/remove; `src/lib/broadcast.ts` (list / add / import / `resolveAudience` / `renderMerge` / unsubscribe). **Stage 2** (composer + batched send + `/unsubscribe/<token>`) not yet built. | `supabase/migrations/0021_broadcasts.sql`, `src/lib/broadcast.ts`, `src/app/api/admin/contacts/**`, `src/app/dashboard/contacts/**`, `database.types.ts` | `rel`/`test`/`main` | **On Prod (code) — ⏳ 0021 migration PENDING (Contacts page errors until applied); Stage 2 + Resend Pro/`news.` subdomain/CAN-SPAM footer pending before any send** | — |

| 27 | 2026-08-25 | **Daily Broadcast — Stage 2 (composer + send + unsubscribe).** New admin **Broadcasts** page (`/dashboard/broadcasts`): pick audience (**Agents** — links generated live: `{link}`=/a/slug, `{portal}`=/agent/token, `{first_name}`, `{firm}`; or **Contacts** — `{first_name}`, `{last_name}` + any imported field), compose with an "Insert field" menu, live preview, **Send test to me**, and **Send to all**. Seeded with Richard's **"Eye on the Ball"** letter as a one-click template. Sends via Resend **batch** endpoint (≤100/call) with a **CAN-SPAM footer** (physical address + per-recipient `/unsubscribe/<token>`, POST-confirm so scanners can't auto-unsub); suppression honored on every send. **DORMANT by design:** the Send path is gated on **`BROADCAST_FROM`** (a dedicated `news.` subdomain sender, separate from the transactional `RESEND_FROM`) — until that env var is set, the Send buttons are disabled and nothing can be emailed. Replaces the Word/Excel mail-merge (no export, no stale links). | `src/lib/broadcast.ts`, `src/app/dashboard/broadcasts/**`, `src/app/api/admin/broadcasts/route.ts`, `src/app/unsubscribe/[token]/**`, `src/app/dashboard/page.tsx` (nav), `.env.example` | `rel`/`test`/`main` | **Draft — ships dormant (no `BROADCAST_FROM`); before first real send: Resend Pro + `news.` subdomain, counsel review of bulk-email copy/footer, and confirm the CAN-SPAM address. Note: serverless send loops the audience — fine at ~100–500; revisit a queue/background job for larger lists.** | — |

## Notes
- Source of the disclosure wording: `R Parry Financial LLC - Disclosure for Rate Calculation.docx` (R Parry Financial OneDrive → 06 Pathfinder 1A). Wording grouped into paragraphs; **not paraphrased** — confirm nothing material was dropped.
- How Richard reviews each item: see "sign-off options" — local screen-share, an exported copy doc, or a Netlify **deploy preview** link (preview ≠ production).
- A change is only marked **Live** after it is merged to `main` and the deploy is confirmed.
