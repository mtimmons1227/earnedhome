# Change Requests — Backlog

Captured requests from Richard (and others) to build **later**. This is the "parking lot" — logging something here does **not** mean it's started. Triage and schedule from here.

> Different from [`CHANGE_SIGNOFF_LOG.md`](CHANGE_SIGNOFF_LOG.md): that log is for changes already **built** and awaiting sign-off before Production. This one is for requests **before** anything is built.

**Status flow:** `Captured` → `Planned` → `In progress` → `Done` (or `Parked` / `Won't do`).

| ID | Date | Requested by | Change | Area | Priority | Status | Notes |
|----|------|-------------|--------|------|----------|--------|-------|
| CR-001 | 2026-08-04 | Richard | **Rebrand EarnedHome → BuyerBridge** — wordmark + logo, in-app branding/header, emails, disclosures, and the production domain. | Branding | Done (over flag) | Executed per Richard's direction | 🛑 **RISK ON RECORD:** "BUYERBRIDGE" is a LIVE US trademark — Reg. No. 6003449, owned by Dealers United, LLC (adtech SaaS); `buyerbridge.com`/`.ai` taken. Using the same name + similar mark for a related SaaS is a likely-confusion infringement risk — **strongly recommend IP counsel clear it, and secure a domain.** Rebrand implemented in-app on Marvin/Richard's instruction (2026-08-04). Not legal advice. Remaining: production domain, the `.docx` manuals (CR-005), and the Netlify subdomain/repo names (infra, unchanged). |
| CR-002 | 2026-08-04 | Richard | **Richard's site `RealAnswer.Mortgage`** — define the relationship to the buyer tool. | Web / integration | TBD | Captured | Open question to confirm with Richard: does the buyer tool **live under / link from** RealAnswer.Mortgage, co-brand with it, or replace it? Decide before wiring anything. |
| CR-003 | 2026-08-04 | Marvin / Richard | **BuyerBridge logo** — design a primary logo + app icon. | Branding / design | Med | Done (2026-08-25) | Color + reversed logos shipped to `public/brand/buyerbridge-logo*.png` and live on every page (white header bar, blue rule/nav pills). Tagline "Real Answers, Better Decisions" incorporated. Still gated by the CR-001 name-clearance note before external launch. |
| CR-004 | 2026-08-04 | Richard | **Rebrand "Loan Officer" → "Mortgage Advisor" system-wide** — all user-facing copy (buyer pages, dashboard, login, emails, errors, disclosures). | Copy / branding | — | Done (pending 2 flags) | ⚠️ **Compliance:** "loan officer" is the NMLS-licensed title; a few disclosure lines now read "licensed Mortgage Advisor" — **Richard to confirm his state permits the marketing title in disclosures.** Internal identifiers, the `/dashboard/los` URL, and the `lo` role are unchanged (not user-visible). |
| CR-005 | 2026-08-04 | (follow-up) | **Rename the "Loan Officer Manual" .docx** to "Mortgage Advisor Manual" — the onboarding email now references the new name. | Docs | Low | Captured | Keep the manual's title in sync with the CR-004 rebrand so the emailed label matches the actual document. |
| CR-006 | 2026-08-25 | Marvin | **Multi-tenant data isolation — verify & harden before a 2nd company/LO.** Confirm one company (tenant) admin can never see another tenant's leads/agents/contacts; decide within-tenant scoping for the broadcast **Contacts** list + agent audience (company-wide vs per-LO). | Security / architecture | High | Captured | Cross-tenant isolation is enforced **today** via `.eq('tenant_id', …)` on every query, and admin-only gating on the Contacts page. **Nuance to address:** the Contacts/broadcast lib (`src/lib/broadcast.ts`) uses the **service-role** client, which **bypasses RLS** — so isolation depends entirely on the code's tenant filter; a single missing filter would leak across tenants. Before onboarding a 2nd lender: (a) add defense-in-depth **RLS policies** on `contacts`/`broadcasts`/etc., (b) seed a 2nd test tenant and prove isolation end-to-end, (c) decide whether Contacts + the broadcast agent-audience stay company-wide/admin-only or become per-LO. **Terminology:** a *tenant* = a mortgage company (R Parry); "Keller Williams" = an agent/**firm** under R Parry (a referral partner), not a tenant — R Parry sees its own KW agents by design; another lender's data stays invisible. |

## Notes
- Nothing here is committed to a release until it graduates to the sign-off log and gets built + approved.
- When an item starts, move it to `In progress` here and (once built) open a matching row in `CHANGE_SIGNOFF_LOG.md`.
